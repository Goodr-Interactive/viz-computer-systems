import React from "react";
import type { ThreadEvent } from "../types";


interface Props {
    event: ThreadEvent;
}


export const EventDisplay : React.FunctionComponent<Props> = ({
    event
}) => {
    return (
        <div>
        </div>
    )
}
